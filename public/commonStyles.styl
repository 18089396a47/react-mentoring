* 
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Helvetica';

[class$=button]
  border-radius: 3px;
  text-transform: uppercase;
  font-weight: bold;
  outline: 0;

.content
  min-height: calc(100vh - 50px);
